// Here, we define a class called MyRoutes that contains static string variables representing the routes used in the application. These routes are used to navigate between different pages in the app. The homeRoute variable represents the route for the home page, while the loginRoute variable represents the route for the login page.

class MyRoutes {
  static String homeRoute = "/home";
  static String loginRoute = "/login";
}